# NexSci SMP — Oracle Bridge v10.0
# Copy to .env and fill in real values. NEVER commit .env to version control.

# ── Authentication ───────────────────────────────────────────────────────────
API_SECRET_KEY=

# ── RCON (Minecraft remote console) ─────────────────────────────────────────
RCON_PASSWORD=
RCON_PORT=25575
RCON_HOST=127.0.0.1

# ── Minecraft server paths ───────────────────────────────────────────────────
# Absolute path to the directory containing server.jar
MC_SERVER_PATH=/home/ubuntu/minecraft

# Absolute path to latest.log  (usually inside /logs/)
LOG_PATH=/home/ubuntu/minecraft/logs/latest.log

# Name of the server JAR file inside MC_SERVER_PATH
MC_JAR_NAME=paper.jar

# JVM flags — tune Xms/Xmx for your instance RAM
MC_JVM_FLAGS=-Xms1G -Xmx3G -XX:+UseG1GC

# ── Bridge server ────────────────────────────────────────────────────────────
BRIDGE_PORT=4000

# ── Firebase / Firestore (service-account JSON, one line, base64-encoded) ────
# Generate: base64 -w0 serviceAccount.json
FIREBASE_SA_BASE64=
FIREBASE_PROJECT_ID=nexsci-smp

# ── Optional diagnostics ─────────────────────────────────────────────────────
# How often (ms) to push a heartbeat to Firestore
HEARTBEAT_INTERVAL_MS=10000

# Maximum log lines buffered in memory before trimming
LOG_BUFFER_SIZE=300
